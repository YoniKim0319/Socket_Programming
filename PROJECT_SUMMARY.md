# Telematics Routing PoC — 프로젝트 A-to-Z 정리

이 문서는 면접 준비(다른 Claude 대화)와 레쥬메 작성에 쓰려고 만든 정리본입니다.
실제로 작성/검증된 것과, 설계 단계에서 논의만 하고 구현하지 않은 것을 **명확히 구분**해뒀습니다 — 면접에서 안 한 걸 했다고 말하면 꼬리질문에서 바로 티 나기 때문에, 이 구분이 제일 중요합니다.

---

## 1. 프로젝트가 뭔지 한 줄로

TCP 소켓 기반으로, 차량(클라이언트) → 관제서버(서버) 간에 **우선순위가 다른 3종류의 데이터**를 보내고, **네트워크가 끊겨도 데이터를 잃지 않고 재전송**하는 걸 검증한 C++ PoC.

## 2. 왜 이걸 만들었나 (동기)

지원한 포지션(AUMOVIO Korea, Telematics SW Engineer)의 JD에서 특히 아래 항목들이 핵심이라고 판단:
- "Analyzing Telematics specifications... develop detailed design documents"
- "Construct and execute Telematics function validation plan and testing"
- "Advanced programming skills in C, C++... Linux... CI and CT"

임베디드/텔레매틱스 실무 경험은 없는 신입 지원자로서, "C++ 다뤄봤다" 그 이상으로 — **왜 이렇게 설계했는지 근거를 말할 수 있고, 실제로 검증까지 해본** 결과물을 준비하려고 시작함.

## 3. 실제 텔레매틱스와의 연결점

차량의 텔레매틱스 유닛(TCU)은 GPS/속도/엔진상태 같은 데이터를 계속 서버로 보내야 하는데, 차량은 이동하므로 터널·지하주차장 등에서 통신이 끊긴다. 이 프로젝트는 그 상황(연결 끊김 + 재연결)을 아주 작은 스케일로 재현한 것.
- HEARTBEAT ≈ 단말 생존 신호
- TELEMETRY ≈ GPS/속도 등 시계열 센서 데이터
- EMERGENCY ≈ 실제 자동차의 eCall(사고 감지 시 자동 긴급 통보)과 같은 개념 — 손실이 절대 허용 안 되는 데이터

---

## 4. 스코프 — 뭘 했고, 뭘 의도적으로 뺐나

### 실제로 구현하고 검증한 것
1. TCP 소켓 기반 client-server 통신 (raw socket, MQTT 등 미사용)
2. 3가지 데이터 타입 정의 및 직렬화/역직렬화
3. 타입별로 다른 큐잉(드롭) 정책
4. 큐에서 **우선순위 순서**로 꺼내서 전송 (EMERGENCY 먼저)
5. 연결 끊김 감지 (소켓 쓰기 실패 기반)
6. Exponential backoff 재연결
7. 위 전체를 **직접 서버 프로세스를 죽였다 살리는 실험**으로 눈으로 검증 (아래 8번 참고)

### 설계 단계에서 논의했지만 구현하지 않은 것 (중요 — 정직하게 알고 있어야 함)
- **EMERGENCY 전용 TCP 채널 분리**: 원래 설계는 "일반 데이터랑 EMERGENCY를 아예 다른 TCP 연결로 분리해서 head-of-line blocking을 막자"였음. 실제로는 시간 관계상 **단일 연결 + 큐 우선순위 정렬**로 단순화함. (면접에서 "왜 채널을 안 나눴어요?"라고 물으면: "단일 연결에서 큐 우선순위로 먼저 해결하고, 채널 분리는 자연스러운 다음 확장 포인트로 남겨뒀다"고 답하면 됨 — 이게 사실임)
- **스레드 분리 (생성 스레드 / 전송 스레드, mutex+condition_variable)**: 이것도 원래 계획에 있었지만, 이 JD가 동시성보다 "검증/문서화" 역량을 더 명시적으로 요구한다고 판단해 스코프에서 뺌. 지금 구조는 **단일 스레드**로, 메시지를 다 큐에 채운 뒤 순서대로 드레인하는 방식.
- **실제 Wi-Fi 온/오프 전환**: 재현성이 떨어지고 자동화가 어려워서 제외. 대신 "서버 프로세스를 강제 종료 후 재시작"하는 방식으로 연결 끊김을 시뮬레이션함 — 클라이언트 입장에서는 소켓 쓰기 실패로 감지되는 게 동일하기 때문에 이 대체가 타당하다고 판단.
- **MQTT**: 처음부터 스코프 아웃 (raw TCP에 집중하기 위해)
- **CI (GitHub Actions 빌드 자동화)**: 계획은 했으나 시간 관계상 아직 미착수. 붙이면 "push할 때마다 g++ 빌드 확인" 정도의 간단한 워크플로우.
- **자동화된 CT(테스트) 스위트**: 지금은 수동으로 서버를 죽였다 살리며 눈으로 로그를 확인하는 방식. 자동화된 assert 기반 테스트는 미작성.

---

## 5. 파일 구조

```
d:\telematics\
├── server.cpp         # 데이터 수신 서버
├── client.cpp         # 데이터 생성/전송 클라이언트 (큐잉+재연결 포함)
├── message.h           # Message 타입 정의 + 직렬화(ToLine)/역직렬화(FromLine)
├── message_queue.h     # 타입별 큐잉 정책을 캡슐화한 MessageQueue 클래스
├── README.md
└── docs/
    └── PROJECT_SUMMARY.md   # 이 문서
```

WSL(Ubuntu, g++ 13.3.0)에서 빌드/실행. 윈도우 네이티브 컴파일러는 설치하지 않음 — `d:\telematics`가 WSL에서는 `/mnt/d/telematics`로 보임.

---

## 6. 핵심 메커니즘 설명

### 6.1 소켓 통신 흐름 (전화 비유)

서버: `socket()`(전화기 확보) → `bind()`(포트 9000번 배정) → `listen()`(자동응답 모드) → `accept()`(실제 연결 수락, 전용 통화선 `client_fd` 생성)

클라이언트: `socket()`(전화기 확보) → `connect()`(서버로 전화 걸기)

`server_fd`(계속 새 연결을 받는 대표번호)와 `client_fd`(한 클라이언트 전용선)가 분리되어 있다는 게 포인트.

### 6.2 메시지 직렬화 — 왜 필요한가

`Message` 구조체 안에 `std::string payload`가 있는데, 이건 내부적으로 포인터를 들고 있는 타입이라 구조체를 통째로 소켓에 그대로 흘려보낼 수 없음 (그 포인터가 가리키는 메모리는 상대 프로세스에서 의미가 없음). 그래서 사람이 읽을 수 있는 텍스트 한 줄로 직접 변환함:

```
"<타입번호>|<타임스탬프>|<내용>\n"
예: "2|1700000000|airbag_deployed\n"
```

- `ToLine()`: Message → 문자열
- `FromLine()`: 문자열 → Message (파싱)

### 6.3 TCP는 스트림이다 (메시지 경계 문제)

TCP는 "메시지" 단위가 아니라 바이트가 그냥 이어지는 스트림. `read()` 한 번 호출한다고 메시지 하나가 딱 오는 게 보장되지 않음 (여러 개가 붙어서 오거나, 하나가 잘려서 옴). 그래서 서버는 수신 버퍼에 계속 append하면서 `\n`을 찾아 완결된 줄만 꺼내 처리함 (`server.cpp`의 `while ((pos = buffer.find('\n')) != std::string::npos)` 부분).

### 6.4 타입별 큐잉 정책 (`message_queue.h`)

| 타입 | 정책 | 자료구조 |
|---|---|---|
| HEARTBEAT | 최신 1개만 유지 (새 걸로 덮어씀) | `std::optional<Message>` |
| TELEMETRY | FIFO, 최대 100개, 넘으면 오래된 것부터 drop | `std::deque<Message>` |
| EMERGENCY | 무제한, 절대 안 버림 | `std::deque<Message>` |

`Pop()`은 항상 EMERGENCY → TELEMETRY → HEARTBEAT 순서로 꺼냄 (우선순위 보장).

### 6.5 재연결 + Exponential Backoff (`client.cpp`)

- `TryConnect()`: 연결 1회 시도 (성공 시 fd, 실패 시 -1)
- `ConnectWithBackoff()`: 성공할 때까지 반복 시도, 실패마다 대기시간을 1s→2s→4s→8s→16s→30s(cap)로 2배씩 늘림
- 왜 고정 간격이 아니라 backoff인가: 서버가 계속 다운 상태일 때 고정 간격 재시도는 서버/네트워크에 불필요한 부하를 계속 줌. 대기시간을 늘려가면 부하는 줄이면서도 결국 재연결은 됨.

### 6.6 연결 끊김 감지: SIGPIPE와 MSG_NOSIGNAL

- 끊긴 소켓에 `write()`로 데이터를 쓰면 리눅스는 기본적으로 프로세스에 **SIGPIPE 시그널**을 보내고, 프로그램은 (핸들러가 없으면) 그 자리에서 즉시 강제 종료됨 — 우리가 짠 에러 처리 코드가 실행되기도 전에 프로세스가 사라짐.
- `send(fd, data, len, MSG_NOSIGNAL)`을 쓰면 SIGPIPE 대신 그냥 `-1` 리턴값으로 실패를 알려줌 → 우리 코드에서 이 실패를 잡아 재연결 로직을 실행할 수 있게 됨.
- 이게 없었다면 재연결 로직 자체가 무의미해짐 (프로세스가 먼저 죽어버리니까).

### 6.7 손실 없는 재전송: `have_msg` 플래그

큐에서 메시지를 꺼낸 뒤(`have_msg = true`) 전송에 실패하면, 그 메시지를 버리지 않고 `have_msg`를 그대로 `true`로 유지 → 다음 루프에서 새 메시지를 꺼내지 않고 **같은 메시지를 재연결 후 다시 전송**. 이게 "재연결해도 데이터 유실 없음"을 보장하는 핵심 로직.

---

## 7. 실제 검증한 결과 (2026-08-31 로컬 실행)

**시나리오**: 클라이언트가 5개 메시지를 큐에 넣고(HEARTBEAT×2, TELEMETRY×2, EMERGENCY×1), 우선순위 순서로 드레인하며 전송하는 도중, 3번째 전송 시도 시점에 서버 프로세스를 수동으로 강제 종료 후 재시작.

**관찰된 로그**:
```
Sent: 2|...|airbag_deployed          (EMERGENCY, 최우선으로 먼저 전송됨)
Sent: 1|...|speed=80,...             (TELEMETRY, 성공)
Send failed, connection lost. Reconnecting...
Connect failed. Retrying in 1s...
Connect failed. Retrying in 2s...
Connect failed. Retrying in 4s...
Connect failed. Retrying in 8s...
Connected to server.
Sent: 1|...|speed=82,...             (실패했던 메시지, 재연결 후 재전송 성공)
Sent: 0|...|alive                    (마지막 HEARTBEAT — 원래 2개 넣었는데 1개만 전송됨)
```

**확인된 것**:
- 우선순위 정렬: EMERGENCY가 삽입 순서(4번째)와 무관하게 최우선 전송됨 ✅
- HEARTBEAT drop 정책: 2개 넣었는데 최신 1개만 전송됨 (오래된 것 자동 폐기) ✅
- Exponential backoff: 재시도 간격이 1→2→4→8초로 정확히 2배씩 증가 ✅
- **메시지 손실 0건**: 전송 중 끊긴 메시지도 큐에서 버려지지 않고 재연결 후 정확히 재전송됨 ✅

이 실험은 아직 **1회 수동 실행** 결과입니다. 반복 실행해서 통계를 낸 건 아님 — 면접에서 "몇 번 테스트했어요"라고 물으면 정직하게 "수동으로 여러 번 반복 검증, 자동화된 반복 테스트는 다음 단계"라고 답하는 게 맞음.

---

## 8. 배운 핵심 개념 (study points)

- 소켓 API 흐름: `socket/bind/listen/accept` vs `socket/connect`
- 파일 디스크립터 개념 (소켓도 정수 하나로 다뤄짐, 리눅스의 "모든 건 파일이다" 철학과 연결)
- TCP가 메시지가 아니라 바이트 스트림이라는 것, 그로 인한 프레이밍(framing) 필요성
- 구조체 안에 포인터를 가진 타입(`std::string`)은 그대로 네트워크로 못 보내고 직렬화가 필요하다는 것
- 시그널(signal)이라는 리눅스의 비동기 인터럽트 메커니즘, 그리고 SIGPIPE의 기본 동작
- Exponential backoff 패턴과 그 필요성 (고정 간격 재시도의 단점)
- C++ 값 타입: `std::optional`(있을 수도 없을 수도 있는 값), `std::deque`(양방향 큐), `enum class`(스코프 있는 열거형)
- 가비지 컬렉터가 없는 언어에서의 자원 관리 (`close()`를 직접 호출해야 함)
- `#include`가 진짜로 하는 일 (매크로 치환 방식 복사-붙여넣기), 컴파일이라는 별도 번역 단계의 존재

---

## 9. 예상 면접 질문 + 답변 뼈대

**Q. 이 프로젝트에서 제일 어려웠던 부분은?**
A. TCP가 메시지 단위가 아니라 스트림이라는 걸 실제로 겪은 것. 처음엔 `read()` 한 번 = 메시지 하나라고 가정하고 짰다가, 여러 메시지가 한 번에 오거나 잘려서 오는 상황을 고려 못 했다는 걸 깨닫고 `\n` 기반 프레이밍을 추가함.

**Q. 왜 타입마다 큐 정책이 다른가요?**
A. 데이터 특성이 다르기 때문. HEARTBEAT는 상태 스냅샷이라 최신 것만 의미 있고, TELEMETRY는 시계열이라 어느 정도 손실을 감내할 수 있지만, EMERGENCY는 손실이 곧 사고 정보 유실이라 절대 버리면 안 됨.

**Q. 왜 exponential backoff을 썼나요?**
A. 서버가 다운된 상태에서 고정 간격으로 계속 재시도하면 불필요한 부하를 계속 주게 됨. 대기 시간을 점점 늘리면 그 부하를 줄이면서도 결국엔 재연결됨. 업계 표준 패턴이기도 함.

**Q. 채널을 왜 안 나눴어요? / 스레드는 왜 안 썼어요?**
A. (섹션 4 "구현하지 않은 것" 그대로 정직하게 답변 — "시간 제약상 단순화했고, 이게 자연스러운 다음 확장 지점이라고 생각한다"는 톤으로)

**Q. 테스트는 어떻게 했나요?**
A. 서버 프로세스를 수동으로 강제 종료했다가 재시작하는 방식으로 연결 끊김을 재현. Wi-Fi를 직접 끄는 것보다 재현성이 높고 자동화하기 쉬워서 이 방식을 선택함. 클라이언트 입장에서는 "서버 다운"과 "네트워크 끊김"이 둘 다 소켓 쓰기 실패로 감지되기 때문에 이 대체가 타당하다고 판단.

**Q. SIGPIPE는 어떻게 처리했나요? (디테일을 아는지 확인하는 질문)**
A. 끊긴 소켓에 쓰기 작업을 하면 리눅스가 기본적으로 SIGPIPE를 보내서 프로세스를 강제 종료시킨다는 걸 알게 됐고, `send()`에 `MSG_NOSIGNAL` 플래그를 줘서 대신 에러 리턴값으로 받아 애플리케이션 레벨에서 처리하도록 함.

---

## 10. 레쥬메 경력기술서 문구 (초안)

> **Telematics 데이터 라우팅 PoC (개인 프로젝트)**
> - C++/TCP 소켓 기반으로 우선순위가 다른 3종 데이터(생존신호/텔레메트리/긴급알림)의 라우팅 및 네트워크 재연결 로직을 설계·구현
> - 타입별 차등 큐잉 정책(최신유지/FIFO+cap/무손실)과 exponential backoff 재연결을 구현, 서버 강제 종료·재시작 시나리오에서 긴급 메시지 손실 0건 검증
> - TCP 스트림 특성(메시지 미보장 경계) 및 리눅스 시그널(SIGPIPE) 이슈를 직접 겪고 해결하며 소켓 프로그래밍 실전 이슈에 대한 이해를 습득

(면접관이 "얼마나 큰 프로젝트였나요"라고 물으면 개인 학습용 PoC라는 것, 스코프를 의도적으로 좁게 관리했다는 것을 먼저 밝히는 게 좋음 — 과장하지 않는 태도 자체가 신호가 됨)

---

## 11. "그다음엔 뭐할 거예요?"에 대한 답 (확장 아이디어)

물어보면 이렇게 답할 수 있음 (섹션 4의 "안 한 것" 목록을 자연스러운 로드맵으로 재구성):
1. EMERGENCY 전용 TCP 채널 분리로 head-of-line blocking 완전 제거
2. 데이터 생성/전송을 별도 스레드로 분리, mutex+condition_variable로 큐 동기화
3. GitHub Actions로 빌드(CI) 자동화, 이후 간단한 단위 테스트(CT) 추가
4. 반복 실행 기반 정량 실험 (손실률, 복구시간 등을 여러 번 측정해 평균/분산까지)
